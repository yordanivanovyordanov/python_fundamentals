words = input().split()
palindrome = input()

found_palindromes = []

for word in words:
    if word == "".join(reversed(word)):
        found_palindromes.append(word)

print(f'{found_palindromes}')
print(f'Found palindrome {found_palindromes.count(palindrome)} times')
